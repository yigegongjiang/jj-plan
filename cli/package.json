{
  "name": "jjplan-cli",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && bun build src/main.ts --compile --target=bun-darwin-arm64 --define JJPLAN_VERSION=$(bun -p \"JSON.stringify(require('fs').readFileSync('../VERSION','utf8').trim())\") --outfile dist/jjplan-macos-arm64",
    "build:local": "mkdir -p $HOME/.local/bin && bun build src/main.ts --compile --define JJPLAN_VERSION=$(bun -p \"JSON.stringify(require('fs').readFileSync('../VERSION','utf8').trim())\") --outfile $HOME/.local/bin/jjplan",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.19.18",
    "typescript": "^5.9.3"
  }
}
